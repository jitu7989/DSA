#include <bits/stdc++.h>
using namespace std;

int main(){

    int n;cin >> n;
    int arr[n]; for (int i = 0; i < n; ++i)  cin >> arr[i];

    int tail=0,head=-1;

    int zero = 0;
    int ans = 0;

    while(tail<n){

        if( (head+1) < n && (arr[head+1]==1 || zero<k)){

        }

    }

    cout << ans << '\n';

}