#include <bits/stdc++.h>
using namespace std;

void solve(){

  int n; cin >> n;
  cout << n-1 << '\n';

}



signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int tc;
    cin >> tc;
    while(tc--) solve();

    return 0;
}